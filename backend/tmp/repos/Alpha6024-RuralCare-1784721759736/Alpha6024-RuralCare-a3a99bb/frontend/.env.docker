# Used when building frontend inside Docker Compose
# The BROWSER runs on the HOST machine, so it must reach
# the backend via localhost (host port 5000), NOT backend:5000
VITE_API_URL=http://localhost:5000/api
